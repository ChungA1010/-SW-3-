__pycache__/
*.py[cod]
.cache/

# Generated inference/training outputs
**/outputs/
**/*.history.json
**/*.metadata.json

# Local datasets and scratch artifacts should stay out of the repo.
single.v1/
single.v2/
single_effect/
multi/

# Keep the trained inference checkpoint via Git LFS, but avoid split manifests.
**/artifacts/split.json
**/artifacts/pretrained/

